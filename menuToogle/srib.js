const button = document.getElementById('tombol');

const nav = document.querySelector('ul');


button.addEventListener('click', function() {
    // muncol.classList.toogle('slide');
    nav.classList.toggle('slide');
});